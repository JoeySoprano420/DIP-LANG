# This is a simple example

start
    var a = 10;
    var b = 20;
    var sum = a + b;
    output(sum);  # Expected output: 30
stop

# Expected Output:

Start Block: Begin execution group.
int a = 10 ; int b = 20 ; int sum = a + b ; std::cout sum ;
Stop Block: End execution group.

